import { CstStrDirective } from './cst-str.directive';

describe('CstStrDirective', () => {
  it('should create an instance', () => {
    const directive = new CstStrDirective();
    expect(directive).toBeTruthy();
  });
});
