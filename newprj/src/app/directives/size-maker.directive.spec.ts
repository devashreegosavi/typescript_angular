import { SizeMakerDirective } from './size-maker.directive';

describe('SizeMakerDirective', () => {
  it('should create an instance', () => {
    const directive = new SizeMakerDirective();
    expect(directive).toBeTruthy();
  });
});
