import { NavbardemoDirective } from './navbardemo.directive';

describe('NavbardemoDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbardemoDirective();
    expect(directive).toBeTruthy();
  });
});
