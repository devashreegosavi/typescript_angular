import { ListingitDirective } from './listingit.directive';

describe('ListingitDirective', () => {
  it('should create an instance', () => {
    const directive = new ListingitDirective();
    expect(directive).toBeTruthy();
  });
});
